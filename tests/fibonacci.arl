int fibonacci(int n) {
    if (n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

for (int i = 0; i < 10; i += 1) {
    println("fibonacci(", i, ") = ", fibonacci(i));
}
